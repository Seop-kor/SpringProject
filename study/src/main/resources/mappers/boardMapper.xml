<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
	<insert id="insert">
		insert into board (title, content, name, pass, writedate, readcount) values (#{title}, #{content}, #{name}, #{pass}, #{writedate}, 0);
	</insert>
</mapper>