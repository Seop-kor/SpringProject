<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
	<insert id="insert">
		insert into board (title, content, name, pass, readcount) values (#{title}, #{content}, #{name}, #{pass}, 0);
	</insert>
	<select id="list" resultType="vo.BoardVO">
		select no, title, name, writedate, readcount from board order by no desc;
	</select>
</mapper>